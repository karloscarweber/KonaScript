// konaconf.h




/* Comparable configuration for the kona frontend. */
#if defined(kona_c)
#define KONA_PROGNAME	"kona"  /* Fallback frontend name. */
#define KONA_PROMPT	"> "	/* Interactive prompt. */
#define KONA_PROMPT2	">> "	/* Continuation prompt. */
#define KONA_MAXINPUT	512	/* Max. input line length. */
#endif
